# More examples

This directory contains several demo clients. They all require the `game_server_api` module found in `client/`. This was solved using symlinks. Depending on your operating system, you may need to copy this module to the respective directory before running a client.
